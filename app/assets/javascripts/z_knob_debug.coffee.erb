<% if Rails.env.test? %>
$ ->
  $('.knob').each ->
    st = $(this).css('transform')
    if st != 'none'
      values = st.split('(')[1].split(')')[0].split(',')
      a = values[0]
      b = values[1]
      c = values[2]
      d = values[3]
      scale = Math.sqrt(a * a + b * b)
      sin = b / scale
      angle = Math.round(Math.atan2(b, a) * 180 / Math.PI)
      $('span.' + $(this).attr('id')).html(angle)
    return
  return
<% end %>
