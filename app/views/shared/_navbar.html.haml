%nav.navbar.navbar-inverse.navbar-fixed-top
  .container
    .navbar-header
      %button.navbar-toggle.collapsed{'aria-controls' => 'navbar', 'aria-expanded' => 'false', 'data-target' => '#navbar', 'data-toggle' => 'collapse', :type => 'button'}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{href: '/'} VolcaShare
    #navbar.navbar-collapse.collapse
      %ul.nav.navbar-nav
        %li
          = link_to 'Bass', '#', class: 'dropdown-toggle', id: 'dropdownMenuLink', data: { toggle: 'dropdown' }
          .dropdown-menu{ aria: { labelledby: 'dropdownMenuLink' } }
            %ul
              %li= link_to 'Browse', patches_path, class: 'dropdown-item'
              %li= link_to 'New patch', new_patch_path, class: 'dropdown-item'
        %li
          = link_to 'Keys', '#', class: 'dropdown-toggle', id: 'dropdownMenuLink', data: { toggle: 'dropdown' }
          .dropdown-menu.keys{ aria: { labelledby: 'dropdownMenuLink' } }
            %ul
              %li= link_to 'Browse', keys_patches_path, class: 'dropdown-item'
              %li= link_to 'New patch', new_keys_patch_path, class: 'dropdown-item'
      %ul.nav.navbar-nav.navbar-right
        - if current_user.present?
          %li= link_to 'My Patches', user_path(current_user.slug)
          %li= link_to 'Log out', destroy_user_session_path, method: :delete
        - else
          %li= link_to 'Log in', new_user_session_path
          %li= link_to 'Sign Up', new_user_session_path
    / /.nav-collapse
