.clearfix
  .col-sm-12#patch-title
    %h1 Edit patch
= render 'midi_control'
= form_tag(patch_path(@patch.slug), method: :put, id: :patch_form) do
  - if @patch.errors.any?
    #error_explanation
      %h2= "#{pluralize(@patch.errors.count, "error")} prohibited this patch from being saved:"
      %ul
        - @patch.errors.full_messages.each do |msg|
          %li= msg

  = render 'form'

.col-sm-12
  = link_to 'Show', @patch
#new_choice_form{:style => "display: none;"}
  = render partial: "sequences/new"
= render 'knob_debug'
